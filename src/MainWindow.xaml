<Window x:Class="Transmission.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Transmission.Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <DataGrid ItemsSource="{Binding Path=Torrents}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" />
                <DataGridTextColumn Header="Done Rate" Binding="{Binding Path=PercentDone, StringFormat=P}" />
                <DataGridTextColumn Header="Down-Speed" Binding="{Binding Path=RateDownload, StringFormat=N0}" />
                <DataGridTextColumn Header="Up-Speed" Binding="{Binding Path=RateUpload, StringFormat=N0}" />
                <DataGridTextColumn Header="Status" Binding="{Binding Path=Status}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
